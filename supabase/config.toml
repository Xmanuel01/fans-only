# Supabase CLI config for this workspace.
# `project_id` is a local identifier for the repo, not your remote project ref.

project_id = "onlyfans"

[api]
enabled = true

[studio]
enabled = true

[functions.upload-creator-profile-asset]
verify_jwt = false
